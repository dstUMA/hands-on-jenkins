#!/usr/bin/groovy

pipeline {
    agent any

    options {
        disableConcurrentBuilds()
    }

    stages {

        stage("Build") {
            steps { buildApp() }
		}

        stage("Deploy - Dev") {
            steps { deploy('dev') }
		}

	}
}


// steps
def buildApp() {
	dir ('section_4/code/cd_pipeline' ) {
		def appImage = docker.build("hands-on-jenkins/myapp:${BUILD_NUMBER}")
	}
}

def deploy(environment) {

    def containerName = ''
    def port = ''

    if (environment == 'dev') {
        containerName = 'app_dev'
        port = '8888'
    } else {
        error "Environment not valid"
    }

    // Stop container if running
    bat """
    FOR /F %%i IN ('docker ps -q -f name=${containerName}') DO docker stop %%i
    """

    // Remove container if exists
    bat """
    FOR /F %%i IN ('docker ps -aq -f name=${containerName}') DO docker rm %%i
    """

    // Run container
    bat """
    docker run -d -p ${port}:5000 --name ${containerName} hands-on-jenkins/myapp:${BUILD_NUMBER}
    """
}




